;; deps-new template library for Polylith monorepos tuned for agentic
;; (AI-pair) Clojure development.
;;
;; Usage:
;;   clojure -Sdeps '{:deps {io.github.brianium/polyllmith {:git/tag "vX.Y.Z" :git/sha "..."}}}' \
;;     -Tnew create :template brianium/polyllmith :name myorg/myapp
;;
;; See README.md for full instructions.
{:paths ["resources"]}
